<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.teamnine.voice"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".VoiceActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".tab1.VoicePlayerMainView"
            android:label="voicePlayer"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab2.ListMain"
            android:label="listMain"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab2.CategoryListView"
            android:label="categoryList"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab2.VoiceListView"
            android:label="VoiceList"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab3.FavoritesListView"
            android:label="favoritesList"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab4.NotePadListView"
            android:label="notePadList"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".tab5.AppSetting"
            android:label="appSetting"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>